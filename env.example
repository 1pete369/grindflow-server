# Backend environment template
# Copy this file to `.env` locally, or paste keys into Render’s Environment section.

# --- Server ---
# Port the server listens on. Render provides PORT automatically in production.
PORT=5001
# Node environment (development|production)
NODE_ENV=development

# --- CORS / Client Origins ---
# Comma-separated list of allowed origins for CORS (no spaces unless URL-encoded).
# Example: https://your-client.onrender.com,http://localhost:3000
CORS_ORIGIN=http://localhost:3000
# Optional: single client origin for Socket.IO CORS (first item of CORS_ORIGIN is used if unset)
CLIENT_ORIGIN=http://localhost:3000

# --- Database ---
# MongoDB connection string
MONGODB_URI=

# --- Auth ---
# Secret used to sign JWTs
JWT_SECRET=

# --- Health / Self Ping (to mitigate free-tier cold starts) ---
# External URL of this service in production (Render sets this automatically)
RENDER_EXTERNAL_URL=
# Optional override for ping base URL; if empty uses RENDER_EXTERNAL_URL or localhost
SELF_PING_URL=
# Minutes between pings (default 14)
PING_INTERVAL_MIN=14
# Set to true to disable self pinging
DISABLE_SELF_PING=false
