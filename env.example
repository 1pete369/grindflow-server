# ENV SETUP (Backend)
# Copy as .env and fill ONLY the REQUIRED section to run.

# === REQUIRED ===
# Your MongoDB connection string
MONGODB_URI=
# Any long random string (used to sign JWTs)
JWT_SECRET=
# Your frontend URL(s) for CORS. Comma-separate if multiple.
# Example: https://your-client.onrender.com,http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# === OPTIONAL (usually leave as-is locally) ===
# Port to run locally (Render sets PORT automatically)
PORT=5001
# Node environment (development|production)
NODE_ENV=development
# If you want to override Socket.IO CORS origin (otherwise first CORS_ORIGIN is used)
CLIENT_ORIGIN=http://localhost:3000

# Keep-alive pings (helpful but optional)
# Public URL of this service (Render sets RENDER_EXTERNAL_URL automatically)
RENDER_EXTERNAL_URL=
# Force a specific URL to ping (defaults to RENDER_EXTERNAL_URL or localhost)
SELF_PING_URL=
# Ping interval in minutes (default 14)
PING_INTERVAL_MIN=14
# Set to true to disable self-pinging entirely
DISABLE_SELF_PING=false
